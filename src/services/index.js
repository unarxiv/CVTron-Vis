import { getHardwareInfo } from './hardware'
import { classify,
  getLog,
  detect,
  startTrain,
  segment,
  getNodeStatus,
  getTrainConfig } from './tasks'

export {
  getHardwareInfo,
  classify,
  getLog,
  detect,
  segment,
  startTrain,
  getTrainConfig,
  getNodeStatus
}
