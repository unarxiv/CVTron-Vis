import { getHardwareInfo } from './hardware'
import { classify, getLog, detect, segment, getTrainConfig } from './tasks'

export {
  getHardwareInfo,
  classify,
  getLog,
  detect,
  segment,
  getTrainConfig
}
