import { getHardwareInfo } from './hardware'
import { classify,
  getLog,
  detect,
  startTrain,
  segment,
  getInferConfig,
  getNodeStatus,
  getTrainConfig } from './tasks'

export {
  getHardwareInfo,
  classify,
  getLog,
  detect,
  getInferConfig,
  segment,
  startTrain,
  getTrainConfig,
  getNodeStatus
}
